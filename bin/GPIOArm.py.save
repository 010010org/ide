from roboarm import Arm
import RPi.GPIO as GGPIO.setmode(GPIO.BOARD)

M1 = (2,3)
M2 = (14, 15)
M3 = (17, 18)
M4 = (27, 22)
M5 = (23, 24)
channel_list = [M1, M2, M3, M4, M5]

GPIO.setup(channel_list), GPIO.OUT)
GPIO.output(channel_list, GPIO.HIGH)

